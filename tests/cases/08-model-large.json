{
  "name": "model-large",
  "description": "Model class 'large' maps to harness-specific model name",
  "command": "agentctl run -h {harness} --headless --prompt \"x\" --model large",
  "skipLive": true,
  "expected": {
    "claude": {
      "dryRun": "--model haiku"
    },
    "opencode": {
      "dryRun": "-m opencode/big-pickle"
    }
  }
}
